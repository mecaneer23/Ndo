-1 fix strikethrough
-2 make editing work on VT100 terminals (alacritty/xterm)
-3 fix redo
-4 implement multiline todos (not with newlines, just really long todos)
  -4 allow multiline paste
  -4 allow multiline print
  -4 allow wgetnstr to use multiline editing
- add `:` prompt to modify constants
- add config variable defaults in README
  - so the user doesn't need to use flags every time
+ simplify main()
- simplify wgetnstr()
- make all undo/redo use reset_todos() - simplify